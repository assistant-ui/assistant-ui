---
"@assistant-ui/core": patch
---

fix: report cloud engagement events once per thread list instead of once per mounted thread, and keep counting a run that ends while its thread is in the background
