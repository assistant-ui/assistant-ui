---
"@assistant-ui/use-generative": patch
---

feat: add @assistant-ui/use-generative — compiler for the `"use generative"` directive that colocates a tool's schema, server-only `execute`, and client-only `render` in one file
