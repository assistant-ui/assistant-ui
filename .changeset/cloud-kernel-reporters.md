---
"assistant-cloud": patch
---

feat: add `CloudRunReporter`, `CloudEngagementReporter` and the `assistant-cloud/ai-sdk` entry (`aiSDKV6FormatAdapter`, `extractAISDKRunTelemetry`), the framework free parts of a cloud integration that `@assistant-ui/core` and `@assistant-ui/cloud-ai-sdk` each carried a copy of
