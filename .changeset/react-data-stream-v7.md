---
"@assistant-ui/react-data-stream": patch
---

feat: support AI SDK v7. bump `@ai-sdk/provider` to `^4` (the v7-line provider spec; the `LanguageModelV2*` types the converter imports are retained).
