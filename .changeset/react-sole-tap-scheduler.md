---
"@assistant-ui/store": patch
"@assistant-ui/react": patch
---

refactor: the config path rides React's scheduler instead of a self-scheduled tap root; top-anchor turn clears skip turns that are still valid
