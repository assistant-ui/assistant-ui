---
"tw-shimmer": patch
---

perf: run text shimmer highlights on the compositor without duplicate markup. The supported path uses an additive band, so tinted surfaces can differ from the gradient fallback.
