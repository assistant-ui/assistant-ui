---
"@assistant-ui/cloud-ai-sdk": patch
---

feat: support AI SDK v7. bump `ai` to `^7` and `@ai-sdk/react` to `^4` (peer and dev) so the package stays on the same `ai` major as `@assistant-ui/react-ai-sdk`.
