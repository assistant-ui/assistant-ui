---
"@assistant-ui/core": patch
"@assistant-ui/react": patch
---

feat: `useAssistantTransportRuntime` accepts `cloud`: Assistant Cloud backs the thread list and every request carries the cloud thread id
