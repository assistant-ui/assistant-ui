{
  "name": "mcp-weather-ui-example",
  "version": "0.0.1",
  "private": true,
  "description": "Example MCP server with Tool UI components",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/server.js",
    "build:ui": "esbuild src/ui/index.ts --bundle --outfile=dist/bundle.js --format=esm --minify"
  },
  "dependencies": {
    "@assistant-ui/tool-ui-server": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.23.0",
    "zod": "^3.25.56"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0"
  }
}
