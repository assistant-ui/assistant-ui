# Agent Playground Components

This directory contains the live same-origin Agent mode for the existing /playground page. The frontend ports the designed assistant-ui Agent shell, landing page, template browser, chat pane, canvas, preview/export controls, workspace environment capture, approvals, questions, and event history into the docs app.
