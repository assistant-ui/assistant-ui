---
title: Styling and Theming
description: Create beautiful, branded chat interfaces with custom styling and themes
---

# Styling and Theming

Assistant UI is headless by default, giving you complete control over styling. This module covers creating beautiful themes, implementing dark mode, responsive design, and building branded chat interfaces.

## CSS-in-JS Styling

Style components with popular CSS-in-JS libraries:

```tsx
import styled from 'styled-components';
import { ThreadPrimitive, MessagePrimitive } from '@assistant-ui/react';

const StyledThread = styled(ThreadPrimitive.Root)`
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
`;

const StyledMessage = styled(MessagePrimitive.Root)`
  display: flex;
  gap: 0.75rem;
  margin-bottom: 1.5rem;
  
  &[data-by="user"] {
    flex-direction: row-reverse;
    .message-content {
      background: #007bff;
      color: white;
      border-radius: 18px 18px 4px 18px;
    }
  }
  
  &[data-by="assistant"] {
    .message-content {
      background: white;
      color: #333;
      border-radius: 18px 18px 18px 4px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
  }
`;

function StyledChat() {
  return (
    <StyledThread>
      <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-4">
        <StyledMessage>
          <MessagePrimitive.Content className="message-content max-w-[70%] p-3" />
        </StyledMessage>
      </ThreadPrimitive.Messages>
    </StyledThread>
  );
}
```

## Tailwind CSS Themes

Create comprehensive themes with Tailwind CSS:

```tsx
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        chat: {
          primary: '#3b82f6',
          secondary: '#6b7280',
          background: '#f9fafb',
          surface: '#ffffff',
          'user-message': '#3b82f6',
          'assistant-message': '#ffffff',
          border: '#e5e7eb'
        }
      },
      animation: {
        'typing': 'typing 1.5s infinite',
        'fade-in': 'fadeIn 0.3s ease-in-out',
        'slide-up': 'slideUp 0.3s ease-out'
      },
      keyframes: {
        typing: {
          '0%, 60%': { opacity: '1' },
          '30%': { opacity: '0.5' }
        },
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' }
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' }
        }
      }
    }
  }
};

// Chat components with Tailwind
function TailwindChat() {
  return (
    <ThreadPrimitive.Root className="flex flex-col h-screen bg-chat-background">
      {/* Header */}
      <div className="flex items-center justify-between p-4 bg-white border-b border-chat-border">
        <h1 className="text-lg font-semibold text-gray-900">AI Assistant</h1>
        <div className="flex items-center gap-2">
          <span className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
          <span className="text-sm text-gray-600">Online</span>
        </div>
      </div>
      
      {/* Messages */}
      <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-4 space-y-4">
        <MessageComponent />
      </ThreadPrimitive.Messages>
      
      {/* Composer */}
      <div className="border-t border-chat-border bg-white p-4">
        <ComposerComponent />
      </div>
    </ThreadPrimitive.Root>
  );
}

function MessageComponent() {
  return (
    <MessagePrimitive.Root className="group animate-slide-up">
      {/* User messages */}
      <MessagePrimitive.If by="user">
        <div className="flex justify-end">
          <div className="max-w-[70%] bg-chat-user-message text-white rounded-2xl rounded-br-md px-4 py-2">
            <MessagePrimitive.Content />
            <div className="text-xs opacity-75 mt-1">
              <MessagePrimitive.Timestamp />
            </div>
          </div>
        </div>
      </MessagePrimitive.If>
      
      {/* Assistant messages */}
      <MessagePrimitive.If by="assistant">
        <div className="flex gap-3">
          <div className="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white text-sm font-bold">
            AI
          </div>
          <div className="flex-1">
            <div className="max-w-[70%] bg-chat-assistant-message border border-chat-border rounded-2xl rounded-tl-md px-4 py-2 shadow-sm">
              <MessagePrimitive.Content />
            </div>
            
            {/* Actions */}
            <div className="flex items-center gap-2 mt-2 opacity-0 group-hover:opacity-100 transition-opacity">
              <ActionBarPrimitive.Copy className="p-1 hover:bg-gray-100 rounded text-gray-500 hover:text-gray-700">
                📋
              </ActionBarPrimitive.Copy>
              <ActionBarPrimitive.Reload className="p-1 hover:bg-gray-100 rounded text-gray-500 hover:text-gray-700">
                🔄
              </ActionBarPrimitive.Reload>
            </div>
          </div>
        </div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}
```

## Dark Mode Implementation

Implement comprehensive dark mode support:

```tsx
import { createContext, useContext, useEffect, useState } from 'react';

const ThemeContext = createContext({
  theme: 'light',
  toggleTheme: () => {}
});

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  
  useEffect(() => {
    // Check system preference and stored preference
    const stored = localStorage.getItem('theme');
    const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    
    if (stored) {
      setTheme(stored as 'light' | 'dark');
    } else if (systemPrefersDark) {
      setTheme('dark');
    }
  }, []);
  
  useEffect(() => {
    document.documentElement.classList.toggle('dark', theme === 'dark');
    localStorage.setItem('theme', theme);
  }, [theme]);
  
  const toggleTheme = () => {
    setTheme(prev => prev === 'light' ? 'dark' : 'light');
  };
  
  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export const useTheme = () => useContext(ThemeContext);

// Dark mode CSS classes
const darkModeClasses = {
  thread: "bg-gray-900 text-white",
  message: {
    user: "bg-blue-600 text-white",
    assistant: "bg-gray-800 border-gray-700 text-gray-100"
  },
  composer: "bg-gray-800 border-gray-700",
  input: "bg-gray-700 text-white placeholder-gray-400 border-gray-600"
};

function DarkModeChat() {
  const { theme } = useTheme();
  
  return (
    <ThreadPrimitive.Root 
      className={`flex flex-col h-screen transition-colors duration-200 ${
        theme === 'dark' 
          ? 'bg-gray-900 text-white' 
          : 'bg-gray-50 text-gray-900'
      }`}
    >
      <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-4">
        <MessagePrimitive.Root>
          <MessagePrimitive.If by="user">
            <div className={`max-w-[70%] ml-auto rounded-2xl px-4 py-2 ${
              theme === 'dark' 
                ? 'bg-blue-600 text-white' 
                : 'bg-blue-500 text-white'
            }`}>
              <MessagePrimitive.Content />
            </div>
          </MessagePrimitive.If>
          
          <MessagePrimitive.If by="assistant">
            <div className={`max-w-[70%] rounded-2xl px-4 py-2 ${
              theme === 'dark' 
                ? 'bg-gray-800 border border-gray-700 text-gray-100' 
                : 'bg-white border border-gray-200 text-gray-900'
            }`}>
              <MessagePrimitive.Content />
            </div>
          </MessagePrimitive.If>
        </MessagePrimitive.Root>
      </ThreadPrimitive.Messages>
    </ThreadPrimitive.Root>
  );
}

// Theme toggle button
function ThemeToggle() {
  const { theme, toggleTheme } = useTheme();
  
  return (
    <button
      onClick={toggleTheme}
      className={`p-2 rounded-lg transition-colors ${
        theme === 'dark'
          ? 'bg-gray-800 text-yellow-400 hover:bg-gray-700'
          : 'bg-gray-200 text-gray-600 hover:bg-gray-300'
      }`}
      aria-label="Toggle theme"
    >
      {theme === 'dark' ? '☀️' : '🌙'}
    </button>
  );
}
```

## Custom Theme System

Build a flexible theme system:

```tsx
interface Theme {
  name: string;
  colors: {
    primary: string;
    secondary: string;
    background: string;
    surface: string;
    text: string;
    textSecondary: string;
    border: string;
    userMessage: string;
    assistantMessage: string;
    error: string;
    success: string;
  };
  spacing: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
  };
  borderRadius: {
    sm: string;
    md: string;
    lg: string;
    full: string;
  };
  typography: {
    fontFamily: string;
    fontSize: {
      sm: string;
      base: string;
      lg: string;
      xl: string;
    };
  };
}

const themes: Record<string, Theme> = {
  default: {
    name: 'Default',
    colors: {
      primary: '#3b82f6',
      secondary: '#6b7280',
      background: '#ffffff',
      surface: '#f9fafb',
      text: '#111827',
      textSecondary: '#6b7280',
      border: '#e5e7eb',
      userMessage: '#3b82f6',
      assistantMessage: '#ffffff',
      error: '#ef4444',
      success: '#10b981'
    },
    spacing: {
      xs: '0.25rem',
      sm: '0.5rem',
      md: '1rem',
      lg: '1.5rem',
      xl: '2rem'
    },
    borderRadius: {
      sm: '0.25rem',
      md: '0.5rem',
      lg: '0.75rem',
      full: '9999px'
    },
    typography: {
      fontFamily: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif',
      fontSize: {
        sm: '0.875rem',
        base: '1rem',
        lg: '1.125rem',
        xl: '1.25rem'
      }
    }
  },
  
  corporate: {
    name: 'Corporate',
    colors: {
      primary: '#1e40af',
      background: '#f8fafc',
      text: '#1e293b',
      userMessage: '#1e40af',
      assistantMessage: '#ffffff',
      border: '#e2e8f0'
    },
    borderRadius: { sm: '0.125rem', md: '0.25rem', lg: '0.375rem' },
    typography: { fontFamily: 'system-ui, sans-serif' }
  },
  
  playful: {
    name: 'Playful',
    colors: {
      primary: '#8b5cf6',
      background: '#fdf4ff',
      text: '#581c87',
      userMessage: '#8b5cf6',
      assistantMessage: '#ffffff',
      border: '#e9d5ff'
    },
    borderRadius: { sm: '0.5rem', md: '1rem', lg: '1.5rem' },
    typography: { fontFamily: 'Comic Neue, cursive' }
  }
};

const ThemeContext = createContext<{
  theme: Theme;
  themeName: string;
  setTheme: (name: string) => void;
}>({
  theme: themes.default,
  themeName: 'default',
  setTheme: () => {}
});

export function CustomThemeProvider({ children }: { children: React.ReactNode }) {
  const [themeName, setThemeName] = useState('default');
  const theme = themes[themeName];
  
  const setTheme = (name: string) => {
    if (themes[name]) {
      setThemeName(name);
      localStorage.setItem('selectedTheme', name);
    }
  };
  
  useEffect(() => {
    const saved = localStorage.getItem('selectedTheme');
    if (saved && themes[saved]) {
      setThemeName(saved);
    }
  }, []);
  
  // Apply CSS custom properties
  useEffect(() => {
    const root = document.documentElement;
    Object.entries(theme.colors).forEach(([key, value]) => {
      root.style.setProperty(`--color-${key}`, value);
    });
    Object.entries(theme.spacing).forEach(([key, value]) => {
      root.style.setProperty(`--spacing-${key}`, value);
    });
    Object.entries(theme.borderRadius).forEach(([key, value]) => {
      root.style.setProperty(`--radius-${key}`, value);
    });
  }, [theme]);
  
  return (
    <ThemeContext.Provider value={{ theme, themeName, setTheme }}>
      <style jsx global>{`
        body {
          font-family: ${theme.typography.fontFamily};
          background-color: ${theme.colors.background};
          color: ${theme.colors.text};
        }
      `}</style>
      {children}
    </ThemeContext.Provider>
  );
}

// Themed components
function ThemedChat() {
  const { theme } = useContext(ThemeContext);
  
  return (
    <div 
      style={{
        backgroundColor: theme.colors.background,
        color: theme.colors.text,
        fontFamily: theme.typography.fontFamily
      }}
      className="h-screen flex flex-col"
    >
      <ThreadPrimitive.Root>
        <ThreadPrimitive.Messages 
          style={{ padding: theme.spacing.md }}
          className="flex-1 overflow-y-auto"
        >
          <ThemedMessage />
        </ThreadPrimitive.Messages>
      </ThreadPrimitive.Root>
    </div>
  );
}

function ThemedMessage() {
  const { theme } = useContext(ThemeContext);
  
  return (
    <MessagePrimitive.Root style={{ marginBottom: theme.spacing.lg }}>
      <MessagePrimitive.If by="user">
        <div 
          style={{
            backgroundColor: theme.colors.userMessage,
            color: 'white',
            padding: theme.spacing.md,
            borderRadius: theme.borderRadius.lg,
            marginLeft: 'auto',
            maxWidth: '70%'
          }}
        >
          <MessagePrimitive.Content />
        </div>
      </MessagePrimitive.If>
      
      <MessagePrimitive.If by="assistant">
        <div 
          style={{
            backgroundColor: theme.colors.assistantMessage,
            color: theme.colors.text,
            padding: theme.spacing.md,
            borderRadius: theme.borderRadius.lg,
            border: `1px solid ${theme.colors.border}`,
            maxWidth: '70%'
          }}
        >
          <MessagePrimitive.Content />
        </div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}

// Theme selector
function ThemeSelector() {
  const { themeName, setTheme } = useContext(ThemeContext);
  
  return (
    <div className="flex gap-2">
      {Object.entries(themes).map(([key, theme]) => (
        <button
          key={key}
          onClick={() => setTheme(key)}
          className={`px-3 py-2 rounded-lg text-sm transition-colors ${
            themeName === key
              ? 'bg-blue-500 text-white'
              : 'bg-gray-200 hover:bg-gray-300'
          }`}
        >
          {theme.name}
        </button>
      ))}
    </div>
  );
}
```

## Responsive Design

Create responsive chat interfaces using Tailwind's responsive utilities:

```tsx
function ResponsiveChat() {
  return (
    <ThreadPrimitive.Root className="flex flex-col md:flex-row h-screen">
      {/* Sidebar - hidden on mobile */}
      <div className="hidden md:block w-80 border-r bg-gray-50">
        <ThreadList />
      </div>
      
      {/* Main chat area */}
      <div className="flex-1 flex flex-col">
        {/* Mobile header */}
        <div className="md:hidden flex items-center justify-between p-4 border-b bg-white">
          <button className="p-2 hover:bg-gray-100 rounded">☰</button>
          <h1 className="font-semibold">Chat</h1>
          <button className="p-2 hover:bg-gray-100 rounded">⋯</button>
        </div>
        
        <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-2 md:p-4">
          <ResponsiveMessage />
        </ThreadPrimitive.Messages>
        
        <div className="border-t bg-white p-2 md:p-4">
          <ResponsiveComposer />
        </div>
      </div>
    </ThreadPrimitive.Root>
  );
}

function ResponsiveMessage() {
  return (
    <MessagePrimitive.Root className="mb-4">
      <MessagePrimitive.If by="user">
        <div className="flex justify-end">
          <div className="max-w-[85%] sm:max-w-[70%] bg-blue-500 text-white rounded-2xl px-3 py-2">
            <MessagePrimitive.Content />
          </div>
        </div>
      </MessagePrimitive.If>
      
      <MessagePrimitive.If by="assistant">
        <div className="flex gap-2 sm:gap-3">
          <div className="w-6 h-6 sm:w-8 sm:h-8 bg-gray-500 rounded-full" />
          <div className="max-w-[85%] sm:max-w-[70%] bg-white border rounded-2xl px-3 py-2">
            <MessagePrimitive.Content />
          </div>
        </div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}
```

## Animation and Transitions

Add smooth animations with Framer Motion:

```tsx
import { motion, AnimatePresence } from 'framer-motion';

const messageVariants = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -20 }
};

function AnimatedMessage() {
  return (
    <MessagePrimitive.Root>
      <MessagePrimitive.If by="user">
        <motion.div
          variants={messageVariants}
          initial="initial"
          animate="animate"
          exit="exit"
          className="flex justify-end mb-4"
        >
          <div className="max-w-[70%] bg-blue-500 text-white rounded-2xl px-4 py-2">
            <MessagePrimitive.Content />
          </div>
        </motion.div>
      </MessagePrimitive.If>
      
      <MessagePrimitive.If by="assistant">
        <motion.div
          variants={messageVariants}
          initial="initial"
          animate="animate"
          exit="exit"
          className="flex gap-3 mb-4"
        >
          <div className="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full" />
          <div className="max-w-[70%] bg-white border rounded-2xl px-4 py-2">
            <MessagePrimitive.Content />
          </div>
        </motion.div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}

// Typing indicator
function TypingIndicator() {
  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
      className="flex items-center gap-2 text-gray-500 text-sm"
    >
      <div className="flex gap-1">
        {[0, 1, 2].map((i) => (
          <motion.div
            key={i}
            animate={{ scale: [1, 1.2, 1] }}
            transition={{ duration: 1.5, repeat: Infinity, delay: i * 0.2 }}
            className="w-2 h-2 bg-gray-400 rounded-full"
          />
        ))}
      </div>
      <span>AI is typing...</span>
    </motion.div>
  );
}
```

## Best Practices

1. **Design System First** - Establish consistent colors, spacing, and typography
2. **CSS Custom Properties** - Enable dynamic theming with CSS variables
3. **Mobile-First Responsive** - Design for mobile, enhance for desktop
4. **Accessibility** - Ensure proper contrast ratios and keyboard navigation
5. **Performance** - Optimize animations and avoid layout thrashing
6. **Theme Persistence** - Save user preferences across sessions

## Key Takeaways

- Assistant UI's headless architecture gives you complete styling control
- Use CSS-in-JS, Tailwind, or custom themes based on your project needs
- Implement responsive design with mobile-first approach
- Add animations thoughtfully to enhance user experience
- Always consider accessibility in your design decisions

## Next Steps

With beautiful, responsive styling in place, let's explore **Performance Optimization** to ensure your chat interface remains fast and smooth under heavy usage.

Continue to [Performance Optimization →](./14-performance-optimization)
