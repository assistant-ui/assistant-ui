---
title: Styling and Theming
description: Create beautiful, branded chat interfaces with custom styling and themes
---

# Styling and Theming

Assistant UI is headless by default, giving you complete control over styling. This module covers creating beautiful themes, implementing dark mode, responsive design, and building branded chat interfaces.

## CSS-in-JS Styling

Style components with popular CSS-in-JS libraries:

```tsx
import styled from 'styled-components';
import { ThreadPrimitive, MessagePrimitive } from '@assistant-ui/react';

const StyledThread = styled(ThreadPrimitive.Root)`
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  
  @media (max-width: 768px) {
    height: 100dvh;
  }
`;

const MessagesContainer = styled(ThreadPrimitive.Messages)`
  flex: 1;
  overflow-y: auto;
  padding: 1rem;
  
  /* Custom scrollbar */
  &::-webkit-scrollbar {
    width: 8px;
  }
  
  &::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 4px;
  }
  
  &::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.3);
    border-radius: 4px;
    
    &:hover {
      background: rgba(255, 255, 255, 0.5);
    }
  }
`;

const StyledMessage = styled(MessagePrimitive.Root)`
  display: flex;
  gap: 0.75rem;
  margin-bottom: 1.5rem;
  
  /* User messages align right */
  &[data-by="user"] {
    flex-direction: row-reverse;
    
    .message-content {
      background: #007bff;
      color: white;
      border-radius: 18px 18px 4px 18px;
    }
  }
  
  /* Assistant messages align left */
  &[data-by="assistant"] {
    .message-content {
      background: white;
      color: #333;
      border-radius: 18px 18px 18px 4px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
  }
`;

const MessageContent = styled(MessagePrimitive.Content)`
  max-width: 70%;
  padding: 0.75rem 1rem;
  line-height: 1.5;
  word-wrap: break-word;
  
  /* Code blocks */
  pre {
    background: rgba(0, 0, 0, 0.1);
    padding: 0.5rem;
    border-radius: 4px;
    overflow-x: auto;
    margin: 0.5rem 0;
  }
  
  /* Inline code */
  code {
    background: rgba(0, 0, 0, 0.1);
    padding: 0.125rem 0.25rem;
    border-radius: 2px;
    font-family: 'Monaco', 'Consolas', monospace;
  }
`;

function StyledChat() {
  return (
    <StyledThread>
      <MessagesContainer>
        <StyledMessage>
          <Avatar />
          <MessageContent className="message-content" />
        </StyledMessage>
      </MessagesContainer>
      <StyledComposer />
    </StyledThread>
  );
}
```

## Tailwind CSS Themes

Create comprehensive themes with Tailwind CSS:

```tsx
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        chat: {
          primary: '#3b82f6',
          secondary: '#6b7280',
          background: '#f9fafb',
          surface: '#ffffff',
          'user-message': '#3b82f6',
          'assistant-message': '#ffffff',
          border: '#e5e7eb'
        }
      },
      animation: {
        'typing': 'typing 1.5s infinite',
        'fade-in': 'fadeIn 0.3s ease-in-out',
        'slide-up': 'slideUp 0.3s ease-out'
      },
      keyframes: {
        typing: {
          '0%, 60%': { opacity: '1' },
          '30%': { opacity: '0.5' }
        },
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' }
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' }
        }
      }
    }
  }
};

// Chat components with Tailwind
function TailwindChat() {
  return (
    <ThreadPrimitive.Root className="flex flex-col h-screen bg-chat-background">
      {/* Header */}
      <div className="flex items-center justify-between p-4 bg-white border-b border-chat-border">
        <h1 className="text-lg font-semibold text-gray-900">AI Assistant</h1>
        <div className="flex items-center gap-2">
          <span className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
          <span className="text-sm text-gray-600">Online</span>
        </div>
      </div>
      
      {/* Messages */}
      <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-4 space-y-4">
        <MessageComponent />
      </ThreadPrimitive.Messages>
      
      {/* Composer */}
      <div className="border-t border-chat-border bg-white p-4">
        <ComposerComponent />
      </div>
    </ThreadPrimitive.Root>
  );
}

function MessageComponent() {
  return (
    <MessagePrimitive.Root className="group animate-slide-up">
      {/* User messages */}
      <MessagePrimitive.If by="user">
        <div className="flex justify-end">
          <div className="max-w-[70%] bg-chat-user-message text-white rounded-2xl rounded-br-md px-4 py-2">
            <MessagePrimitive.Content />
            <div className="text-xs opacity-75 mt-1">
              <MessagePrimitive.Timestamp />
            </div>
          </div>
        </div>
      </MessagePrimitive.If>
      
      {/* Assistant messages */}
      <MessagePrimitive.If by="assistant">
        <div className="flex gap-3">
          <div className="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white text-sm font-bold">
            AI
          </div>
          <div className="flex-1">
            <div className="max-w-[70%] bg-chat-assistant-message border border-chat-border rounded-2xl rounded-tl-md px-4 py-2 shadow-sm">
              <MessagePrimitive.Content />
            </div>
            
            {/* Actions */}
            <div className="flex items-center gap-2 mt-2 opacity-0 group-hover:opacity-100 transition-opacity">
              <ActionBarPrimitive.Copy className="p-1 hover:bg-gray-100 rounded text-gray-500 hover:text-gray-700">
                📋
              </ActionBarPrimitive.Copy>
              <ActionBarPrimitive.Reload className="p-1 hover:bg-gray-100 rounded text-gray-500 hover:text-gray-700">
                🔄
              </ActionBarPrimitive.Reload>
            </div>
          </div>
        </div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}
```

## Dark Mode Implementation

Implement comprehensive dark mode support:

```tsx
import { createContext, useContext, useEffect, useState } from 'react';

const ThemeContext = createContext({
  theme: 'light',
  toggleTheme: () => {}
});

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  
  useEffect(() => {
    // Check system preference and stored preference
    const stored = localStorage.getItem('theme');
    const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    
    if (stored) {
      setTheme(stored as 'light' | 'dark');
    } else if (systemPrefersDark) {
      setTheme('dark');
    }
  }, []);
  
  useEffect(() => {
    document.documentElement.classList.toggle('dark', theme === 'dark');
    localStorage.setItem('theme', theme);
  }, [theme]);
  
  const toggleTheme = () => {
    setTheme(prev => prev === 'light' ? 'dark' : 'light');
  };
  
  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export const useTheme = () => useContext(ThemeContext);

// Dark mode CSS classes
const darkModeClasses = {
  thread: "bg-gray-900 text-white",
  message: {
    user: "bg-blue-600 text-white",
    assistant: "bg-gray-800 border-gray-700 text-gray-100"
  },
  composer: "bg-gray-800 border-gray-700",
  input: "bg-gray-700 text-white placeholder-gray-400 border-gray-600"
};

function DarkModeChat() {
  const { theme } = useTheme();
  
  return (
    <ThreadPrimitive.Root 
      className={`flex flex-col h-screen transition-colors duration-200 ${
        theme === 'dark' 
          ? 'bg-gray-900 text-white' 
          : 'bg-gray-50 text-gray-900'
      }`}
    >
      <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-4">
        <MessagePrimitive.Root>
          <MessagePrimitive.If by="user">
            <div className={`max-w-[70%] ml-auto rounded-2xl px-4 py-2 ${
              theme === 'dark' 
                ? 'bg-blue-600 text-white' 
                : 'bg-blue-500 text-white'
            }`}>
              <MessagePrimitive.Content />
            </div>
          </MessagePrimitive.If>
          
          <MessagePrimitive.If by="assistant">
            <div className={`max-w-[70%] rounded-2xl px-4 py-2 ${
              theme === 'dark' 
                ? 'bg-gray-800 border border-gray-700 text-gray-100' 
                : 'bg-white border border-gray-200 text-gray-900'
            }`}>
              <MessagePrimitive.Content />
            </div>
          </MessagePrimitive.If>
        </MessagePrimitive.Root>
      </ThreadPrimitive.Messages>
    </ThreadPrimitive.Root>
  );
}

// Theme toggle button
function ThemeToggle() {
  const { theme, toggleTheme } = useTheme();
  
  return (
    <button
      onClick={toggleTheme}
      className={`p-2 rounded-lg transition-colors ${
        theme === 'dark'
          ? 'bg-gray-800 text-yellow-400 hover:bg-gray-700'
          : 'bg-gray-200 text-gray-600 hover:bg-gray-300'
      }`}
      aria-label="Toggle theme"
    >
      {theme === 'dark' ? '☀️' : '🌙'}
    </button>
  );
}
```

## Custom Theme System

Build a flexible theme system:

```tsx
interface Theme {
  name: string;
  colors: {
    primary: string;
    secondary: string;
    background: string;
    surface: string;
    text: string;
    textSecondary: string;
    border: string;
    userMessage: string;
    assistantMessage: string;
    error: string;
    success: string;
  };
  spacing: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
  };
  borderRadius: {
    sm: string;
    md: string;
    lg: string;
    full: string;
  };
  typography: {
    fontFamily: string;
    fontSize: {
      sm: string;
      base: string;
      lg: string;
      xl: string;
    };
  };
}

const themes: Record<string, Theme> = {
  default: {
    name: 'Default',
    colors: {
      primary: '#3b82f6',
      secondary: '#6b7280',
      background: '#ffffff',
      surface: '#f9fafb',
      text: '#111827',
      textSecondary: '#6b7280',
      border: '#e5e7eb',
      userMessage: '#3b82f6',
      assistantMessage: '#ffffff',
      error: '#ef4444',
      success: '#10b981'
    },
    spacing: {
      xs: '0.25rem',
      sm: '0.5rem',
      md: '1rem',
      lg: '1.5rem',
      xl: '2rem'
    },
    borderRadius: {
      sm: '0.25rem',
      md: '0.5rem',
      lg: '0.75rem',
      full: '9999px'
    },
    typography: {
      fontFamily: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif',
      fontSize: {
        sm: '0.875rem',
        base: '1rem',
        lg: '1.125rem',
        xl: '1.25rem'
      }
    }
  },
  
  corporate: {
    name: 'Corporate',
    colors: {
      primary: '#1e40af',
      secondary: '#475569',
      background: '#f8fafc',
      surface: '#ffffff',
      text: '#1e293b',
      textSecondary: '#64748b',
      border: '#e2e8f0',
      userMessage: '#1e40af',
      assistantMessage: '#ffffff',
      error: '#dc2626',
      success: '#059669'
    },
    spacing: {
      xs: '0.25rem',
      sm: '0.5rem',
      md: '1rem',
      lg: '1.5rem',
      xl: '2rem'
    },
    borderRadius: {
      sm: '0.125rem',
      md: '0.25rem',
      lg: '0.375rem',
      full: '9999px'
    },
    typography: {
      fontFamily: 'system-ui, -apple-system, sans-serif',
      fontSize: {
        sm: '0.875rem',
        base: '1rem',
        lg: '1.125rem',
        xl: '1.25rem'
      }
    }
  },
  
  playful: {
    name: 'Playful',
    colors: {
      primary: '#8b5cf6',
      secondary: '#a78bfa',
      background: '#fdf4ff',
      surface: '#ffffff',
      text: '#581c87',
      textSecondary: '#7c3aed',
      border: '#e9d5ff',
      userMessage: '#8b5cf6',
      assistantMessage: '#ffffff',
      error: '#f97316',
      success: '#06d6a0'
    },
    spacing: {
      xs: '0.375rem',
      sm: '0.75rem',
      md: '1.25rem',
      lg: '1.875rem',
      xl: '2.5rem'
    },
    borderRadius: {
      sm: '0.5rem',
      md: '1rem',
      lg: '1.5rem',
      full: '9999px'
    },
    typography: {
      fontFamily: 'Comic Neue, cursive',
      fontSize: {
        sm: '0.875rem',
        base: '1rem',
        lg: '1.125rem',
        xl: '1.25rem'
      }
    }
  }
};

const ThemeContext = createContext<{
  theme: Theme;
  themeName: string;
  setTheme: (name: string) => void;
}>({
  theme: themes.default,
  themeName: 'default',
  setTheme: () => {}
});

export function CustomThemeProvider({ children }: { children: React.ReactNode }) {
  const [themeName, setThemeName] = useState('default');
  const theme = themes[themeName];
  
  const setTheme = (name: string) => {
    if (themes[name]) {
      setThemeName(name);
      localStorage.setItem('selectedTheme', name);
    }
  };
  
  useEffect(() => {
    const saved = localStorage.getItem('selectedTheme');
    if (saved && themes[saved]) {
      setThemeName(saved);
    }
  }, []);
  
  // Apply CSS custom properties
  useEffect(() => {
    const root = document.documentElement;
    Object.entries(theme.colors).forEach(([key, value]) => {
      root.style.setProperty(`--color-${key}`, value);
    });
    Object.entries(theme.spacing).forEach(([key, value]) => {
      root.style.setProperty(`--spacing-${key}`, value);
    });
    Object.entries(theme.borderRadius).forEach(([key, value]) => {
      root.style.setProperty(`--radius-${key}`, value);
    });
  }, [theme]);
  
  return (
    <ThemeContext.Provider value={{ theme, themeName, setTheme }}>
      <style jsx global>{`
        body {
          font-family: ${theme.typography.fontFamily};
          background-color: ${theme.colors.background};
          color: ${theme.colors.text};
        }
      `}</style>
      {children}
    </ThemeContext.Provider>
  );
}

// Themed components
function ThemedChat() {
  const { theme } = useContext(ThemeContext);
  
  return (
    <div 
      style={{
        backgroundColor: theme.colors.background,
        color: theme.colors.text,
        fontFamily: theme.typography.fontFamily
      }}
      className="h-screen flex flex-col"
    >
      <ThreadPrimitive.Root>
        <ThreadPrimitive.Messages 
          style={{ padding: theme.spacing.md }}
          className="flex-1 overflow-y-auto"
        >
          <ThemedMessage />
        </ThreadPrimitive.Messages>
      </ThreadPrimitive.Root>
    </div>
  );
}

function ThemedMessage() {
  const { theme } = useContext(ThemeContext);
  
  return (
    <MessagePrimitive.Root style={{ marginBottom: theme.spacing.lg }}>
      <MessagePrimitive.If by="user">
        <div 
          style={{
            backgroundColor: theme.colors.userMessage,
            color: 'white',
            padding: theme.spacing.md,
            borderRadius: theme.borderRadius.lg,
            marginLeft: 'auto',
            maxWidth: '70%'
          }}
        >
          <MessagePrimitive.Content />
        </div>
      </MessagePrimitive.If>
      
      <MessagePrimitive.If by="assistant">
        <div 
          style={{
            backgroundColor: theme.colors.assistantMessage,
            color: theme.colors.text,
            padding: theme.spacing.md,
            borderRadius: theme.borderRadius.lg,
            border: `1px solid ${theme.colors.border}`,
            maxWidth: '70%'
          }}
        >
          <MessagePrimitive.Content />
        </div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}

// Theme selector
function ThemeSelector() {
  const { themeName, setTheme } = useContext(ThemeContext);
  
  return (
    <div className="flex gap-2">
      {Object.entries(themes).map(([key, theme]) => (
        <button
          key={key}
          onClick={() => setTheme(key)}
          className={`px-3 py-2 rounded-lg text-sm transition-colors ${
            themeName === key
              ? 'bg-blue-500 text-white'
              : 'bg-gray-200 hover:bg-gray-300'
          }`}
        >
          {theme.name}
        </button>
      ))}
    </div>
  );
}
```

## Responsive Design

Create responsive chat interfaces:

```tsx
function ResponsiveChat() {
  const [isMobile, setIsMobile] = useState(false);
  
  useEffect(() => {
    const checkMobile = () => {
      setIsMobile(window.innerWidth < 768);
    };
    
    checkMobile();
    window.addEventListener('resize', checkMobile);
    return () => window.removeEventListener('resize', checkMobile);
  }, []);
  
  return (
    <ThreadPrimitive.Root 
      className={`flex h-screen ${
        isMobile ? 'flex-col' : 'flex-row'
      }`}
    >
      {/* Sidebar - hidden on mobile */}
      {!isMobile && (
        <div className="w-80 border-r bg-gray-50 hidden md:block">
          <ThreadList />
        </div>
      )}
      
      {/* Main chat area */}
      <div className="flex-1 flex flex-col">
        {/* Mobile header */}
        {isMobile && (
          <div className="flex items-center justify-between p-4 border-b bg-white">
            <button className="p-2 hover:bg-gray-100 rounded">
              ☰
            </button>
            <h1 className="font-semibold">Chat</h1>
            <button className="p-2 hover:bg-gray-100 rounded">
              ⋯
            </button>
          </div>
        )}
        
        {/* Messages */}
        <ThreadPrimitive.Messages 
          className={`flex-1 overflow-y-auto ${
            isMobile ? 'p-2' : 'p-4'
          }`}
        >
          <ResponsiveMessage />
        </ThreadPrimitive.Messages>
        
        {/* Composer */}
        <div className={`border-t bg-white ${
          isMobile ? 'p-2' : 'p-4'
        }`}>
          <ResponsiveComposer />
        </div>
      </div>
    </ThreadPrimitive.Root>
  );
}

function ResponsiveMessage() {
  return (
    <MessagePrimitive.Root className="mb-4">
      <MessagePrimitive.If by="user">
        <div className="flex justify-end">
          <div className="max-w-[85%] sm:max-w-[70%] bg-blue-500 text-white rounded-2xl rounded-br-md px-3 py-2 sm:px-4 sm:py-2">
            <MessagePrimitive.Content className="text-sm sm:text-base" />
          </div>
        </div>
      </MessagePrimitive.If>
      
      <MessagePrimitive.If by="assistant">
        <div className="flex gap-2 sm:gap-3">
          <div className="w-6 h-6 sm:w-8 sm:h-8 bg-gray-500 rounded-full flex-shrink-0" />
          <div className="max-w-[85%] sm:max-w-[70%] bg-white border rounded-2xl rounded-tl-md px-3 py-2 sm:px-4 sm:py-2">
            <MessagePrimitive.Content className="text-sm sm:text-base" />
          </div>
        </div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}

function ResponsiveComposer() {
  return (
    <ComposerPrimitive.Root className="flex items-end gap-2">
      <ComposerPrimitive.Input 
        className="flex-1 p-2 sm:p-3 border rounded-lg resize-none text-sm sm:text-base"
        placeholder="Type a message..."
        rows={1}
      />
      <ComposerPrimitive.Send className="p-2 sm:p-3 bg-blue-500 text-white rounded-lg flex-shrink-0">
        <span className="hidden sm:inline">Send</span>
        <span className="sm:hidden">→</span>
      </ComposerPrimitive.Send>
    </ComposerPrimitive.Root>
  );
}
```

## Animation and Transitions

Add smooth animations and transitions:

```tsx
import { motion, AnimatePresence } from 'framer-motion';

function AnimatedChat() {
  return (
    <ThreadPrimitive.Root className="flex flex-col h-screen bg-gray-50">
      <ThreadPrimitive.Messages className="flex-1 overflow-y-auto p-4">
        <AnimatePresence mode="popLayout">
          <AnimatedMessage />
        </AnimatePresence>
      </ThreadPrimitive.Messages>
      
      <AnimatedComposer />
    </ThreadPrimitive.Root>
  );
}

function AnimatedMessage() {
  return (
    <MessagePrimitive.Root>
      <MessagePrimitive.If by="user">
        <motion.div
          initial={{ opacity: 0, x: 20, scale: 0.95 }}
          animate={{ opacity: 1, x: 0, scale: 1 }}
          exit={{ opacity: 0, x: -20, scale: 0.95 }}
          transition={{ duration: 0.3, ease: "easeOut" }}
          className="flex justify-end mb-4"
        >
          <div className="max-w-[70%] bg-blue-500 text-white rounded-2xl rounded-br-md px-4 py-2">
            <MessagePrimitive.Content />
          </div>
        </motion.div>
      </MessagePrimitive.If>
      
      <MessagePrimitive.If by="assistant">
        <motion.div
          initial={{ opacity: 0, x: -20, scale: 0.95 }}
          animate={{ opacity: 1, x: 0, scale: 1 }}
          exit={{ opacity: 0, x: 20, scale: 0.95 }}
          transition={{ duration: 0.3, ease: "easeOut" }}
          className="flex gap-3 mb-4"
        >
          <motion.div
            initial={{ scale: 0 }}
            animate={{ scale: 1 }}
            transition={{ delay: 0.1, duration: 0.2 }}
            className="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full"
          />
          <div className="max-w-[70%] bg-white border rounded-2xl rounded-tl-md px-4 py-2 shadow-sm">
            <MessagePrimitive.Content />
          </div>
        </motion.div>
      </MessagePrimitive.If>
    </MessagePrimitive.Root>
  );
}

function AnimatedComposer() {
  const [isExpanded, setIsExpanded] = useState(false);
  
  return (
    <motion.div
      initial={{ y: 100 }}
      animate={{ y: 0 }}
      transition={{ duration: 0.3, ease: "easeOut" }}
      className="border-t bg-white p-4"
    >
      <ComposerPrimitive.Root className="flex items-end gap-2">
        <motion.div
          animate={{ height: isExpanded ? 120 : 'auto' }}
          transition={{ duration: 0.2 }}
          className="flex-1"
        >
          <ComposerPrimitive.Input
            onFocus={() => setIsExpanded(true)}
            onBlur={() => setIsExpanded(false)}
            className="w-full p-3 border rounded-lg resize-none transition-all duration-200 focus:ring-2 focus:ring-blue-500"
            placeholder="Type your message..."
          />
        </motion.div>
        
        <ComposerPrimitive.Send asChild>
          <motion.button
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
            className="p-3 bg-blue-500 text-white rounded-lg"
          >
            Send
          </motion.button>
        </ComposerPrimitive.Send>
      </ComposerPrimitive.Root>
    </motion.div>
  );
}

// Typing indicator with animation
function TypingIndicator() {
  return (
    <motion.div
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -10 }}
      className="flex items-center gap-2 text-gray-500 text-sm"
    >
      <div className="flex gap-1">
        {[0, 1, 2].map((i) => (
          <motion.div
            key={i}
            animate={{
              scale: [1, 1.2, 1],
              opacity: [0.5, 1, 0.5]
            }}
            transition={{
              duration: 1.5,
              repeat: Infinity,
              delay: i * 0.2
            }}
            className="w-2 h-2 bg-gray-400 rounded-full"
          />
        ))}
      </div>
      <span>AI is typing...</span>
    </motion.div>
  );
}
```

## Best Practices

1. **Start with a design system** for consistency
2. **Use CSS custom properties** for easy theming
3. **Implement responsive design** from the start
4. **Test with different content lengths** and edge cases
5. **Consider accessibility** in color choices and contrast
6. **Optimize animations** for performance
7. **Provide theme persistence** across sessions

## Exercise: Brand Theme System

Create a complete brand theme system that includes:
- Multiple color schemes and typography options
- Dark/light mode with system preference detection
- Responsive design for all screen sizes
- Smooth animations and micro-interactions
- Accessibility-compliant color contrasts
- Custom component variants

## Next Steps

Beautiful styling makes your chat interface professional and engaging. Next, let's explore **Performance Optimization** to ensure your chat remains fast and responsive.

Continue to [Performance Optimization →](./14-performance-optimization)