---
title: "Input"
description: "A single-line field with a label, drawn with a hairline."
links:
  - label: Source
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/input.tsx
---

import { PreviewCode } from "@/components/docs/preview-code.server";
import { KitInstall } from "@/components/design/kit-install";
import {
  InputSpecimen,
  InputWithButtonSpecimen,
} from "@/components/design/specimens";
import * as RadixSpecimens from "@/components/design/specimens.radix";
import { FlavorSwitcher } from "@/components/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/design/specimens" name="InputSpecimen" base={<InputSpecimen />}>
  <RadixSpecimens.InputSpecimen />
</PreviewCode>

## Installation

<KitInstall name="input" />

## Usage

```tsx
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function Example() {
  return (
    <div className="flex flex-col gap-2">
      <Label htmlFor="email">Email</Label>
      <Input id="email" type="email" placeholder="you@example.com" />
    </div>
  );
}
```

## Examples

### With a button

Inputs sit on the same 32px rhythm as buttons, so rows compose without alignment fixes.

<PreviewCode file="components/design/specimens" name="InputWithButtonSpecimen" base={<InputWithButtonSpecimen />}>
  <RadixSpecimens.InputWithButtonSpecimen />
</PreviewCode>

## API Reference

### Input

Accepts every native `input` prop.

<ParametersTable
  type="InputProps"
  parameters={[
    {
      name: "type",
      type: "string",
      default: '"text"',
      description: "The native input type.",
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      description: "Prevents editing and mutes the field.",
    },
    {
      name: "className",
      type: "string",
      description: "Additional CSS classes.",
    },
  ]}
/>
