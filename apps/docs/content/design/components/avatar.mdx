---
title: "Avatar"
description: "People as initials or images, alone or stacked in groups."
links:
  - label: Source
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/avatar.tsx
  - label: Base UI Avatar
    url: https://base-ui.com/react/components/avatar
---

import { PreviewCode } from "@/components/docs/preview-code.server";
import { KitInstall } from "@/components/design/kit-install";
import {
  AvatarSpecimen,
  AvatarSizesSpecimen,
  AvatarBadgeSpecimen,
} from "@/components/design/specimens";
import * as RadixSpecimens from "@/components/design/specimens.radix";
import { FlavorSwitcher } from "@/components/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/design/specimens" name="AvatarSpecimen" base={<AvatarSpecimen />}>
  <RadixSpecimens.AvatarSpecimen />
</PreviewCode>

## Installation

<KitInstall name="avatar" />

## Usage

```tsx
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";

export function Example() {
  return (
    <Avatar>
      <AvatarImage src="/avatar.png" alt="Ada" />
      <AvatarFallback>AD</AvatarFallback>
    </Avatar>
  );
}
```

## Examples

### Sizes

<PreviewCode file="components/design/specimens" name="AvatarSizesSpecimen" base={<AvatarSizesSpecimen />}>
  <RadixSpecimens.AvatarSizesSpecimen />
</PreviewCode>

### Status badge

`AvatarBadge` pins a marker to the rim; give it a background to carry the status.

<PreviewCode file="components/design/specimens" name="AvatarBadgeSpecimen" base={<AvatarBadgeSpecimen />}>
  <RadixSpecimens.AvatarBadgeSpecimen />
</PreviewCode>

## API Reference

### Avatar

Wraps Base UI's Avatar root.

<ParametersTable
  type="AvatarProps"
  parameters={[
    {
      name: "size",
      type: '"sm" | "default" | "lg"',
      default: '"default"',
      description: "24, 32, or 40 pixels.",
    },
  ]}
/>

### Parts

| Part | Description |
|------|-------------|
| `AvatarImage` | The image, shown once it loads. |
| `AvatarFallback` | Initials or an icon while the image is missing. |
| `AvatarBadge` | A status marker pinned to the rim. |
| `AvatarGroup` | Overlapping stack of avatars. |
| `AvatarGroupCount` | Overflow count that closes a group. |
